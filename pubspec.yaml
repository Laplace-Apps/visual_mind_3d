name: visual_mind_3d
description: >
  A pure Flutter 3D interactive force-directed graph view that renders onto a
  Canvas via CustomPainter and Matrix4 — no WebViews.
version: 0.1.0
homepage: https://github.com/Laplace-Apps/visual_mind_3d
repository: https://github.com/Laplace-Apps/visual_mind_3d

environment:
  sdk: ^3.10.7
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
